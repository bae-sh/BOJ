#include<iostream>
#include<vector>

using namespace std;
int V, E;
vector<int> e[10001];
vector<int> rev[10001];

int main() {
	ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);

	cin >> N >> E;
	for (int i = 0; i < E; i++)[
		int a, b; cin >> a >> b;
		e[a].push_back(a);
}